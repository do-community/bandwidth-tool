const app = require('./app');
const explainer = require('./explainer');
const faqs = require('./faqs');
const pool = require('./pool');
const droplets = require('./droplets');

module.exports = { app, explainer, faqs, pool, droplets };
